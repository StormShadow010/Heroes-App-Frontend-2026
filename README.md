# Heroes APP

## Levantar Desarrollo

1. Clonar el repositorio
2. Editar el archivo `.env` con las variable de entorno basado en el archivo `.env.template`
3. Ejecutar `npm install`
4. Ejecuta `npm run dev`
